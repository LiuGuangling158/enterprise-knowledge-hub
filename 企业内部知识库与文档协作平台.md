# 企业内部知识库与文档协作平台 — 技术架构与实施规划

---

## 一、项目背景与价值

### 1.1 背景

以一家100~500人的中型企业为例。团队分布在多个部门：技术部、产品部、市场部、人事部、财务部。每个部门内部都有自己的文档习惯：

- 技术部用飞书文档写技术方案和架构设计
- 产品部用语雀维护产品文档和需求文档
- 人事部用腾讯文档管理员工手册和培训材料
- 市场部的方案在 Google Docs 里，部分在微信群里

当一名新员工入职时，他需要花两周时间去各个平台翻资料、加群、问人，才能知道自己部门有哪些核心文档、公司有哪些规范。三个月后，有经验的老员工离职，他电脑里的历史项目总结和经验记录也随之流失。

这是真实的企业知识碎片化问题。不同规模的企业严重程度不同，但本质是一样的：**知识在增长，但不在一个地方，也无法被快速找到。**

### 1.2 这个项目解决什么

**核心问题一：文档分散，没有统一入口**
员工需要在3~5个不同的平台之间切换来查找信息，且每个平台都有独立的账号体系和权限。搜索一个关键词，要分别在每个平台搜一遍。

本项目的解决方案：统一的文档管理平台，所有知识集中存储、统一搜索。不再需要跨平台切换。

**核心问题二：历史知识难以沉淀，随人员流失**
老员工离职时带走大量隐性知识——项目经验、踩坑记录、决策背景。这些没有文档化的知识一旦流失就无法找回。

本项目的解决方案：文档版本管理与审批发布流程，让知识沉淀为组织资产。同时通过 Agent 对历史文档进行自动摘要和关联分析，挖掘隐性联系。

**核心问题三：检索效率低，找不到想要的内容**
关键词搜索只能匹配标题和标签，无法理解语义。员工输入"去年Q4技术团队的人员变动情况"，只能搜到标题含"人"或"变动"的文档。

本项目的解决方案：多路召回（语义搜索 + 关键词搜索 + 元数据过滤）+ Query 改写，让用户用自然语言就能找到精准内容。

**核心问题四：企业知识无法被"问"出来**
员工有疑问时，传统做法是翻文档或问同事。翻文档慢，问同事打扰人家工作。

本项目的解决方案：多Agent 问答系统。员工直接在平台上输入问题，Agent 从知识库中检索相关内容，组织答案并附上引用来源。不需要翻文档，不需要打扰同事。

**核心问题五：部门间数据隔离难**
财务部的工资单不能给技术部看到，技术部的架构文档不适合全公司公开。没有统一权限体系时，要么管控太松要么管控太死。

本项目的解决方案：多租户行级隔离 + 文档级ACL，每个部门的文档默认只对本部门可见，跨部门访问需授权。

### 1.3 需求分析与功能范围

#### 基础功能

- 用户注册登录：支持企业员工注册、登录、登录态校验与 JWT 鉴权。
- 企业和团队空间：支持企业租户、部门/团队空间与团队内知识隔离。
- 文档上传：支持 Markdown、文本等文档上传解析，并预留 Word/PDF 等结构化解析入口。
- 文档在线管理：支持文档新建、编辑、保存、发布、归档、恢复、评论与版本记录。
- 权限控制：支持租户隔离、部门默认可见、公开可见、作者/管理员编辑与文档级 ACL 扩展。
- 全文搜索：支持按标题、标签、正文进行基础全文检索，并预留语义检索和元数据过滤。
- RAG 问答：基于知识库召回内容组织答案，支持自然语言提问与流式回答。
- 对话历史：保留问答会话上下文与历史消息，支持后续追问和问题回溯。
- 引用来源：回答必须携带来源文档、版本、段落或原文片段，保证可追溯。
- 管理员后台：支持用户、团队、文档、审批、权限、系统配置和数据看板管理。
- Docker 部署：支持通过 Docker Compose 启动前端、后端、数据库、缓存、向量库和对象存储等依赖。

#### 进阶功能

- 自动总结文档：上传或保存文档后自动生成摘要、关键词和推荐标签。
- 会议纪要提取：从会议记录或音视频转写文本中提取议题、决议、待办和责任人。
- 文档版本比较：支持不同版本之间的内容差异对比、变更摘要和回滚依据查看。
- 多 Agent 审核流程：由审核 Agent、检索 Agent、敏感检测 Skill 等协同完成格式、合规、重复内容和敏感信息审查。
- 知识库更新提醒：文档新增、更新、审批通过或关键内容变化时，向相关团队或订阅者发送提醒。
- 操作日志：记录登录、上传、编辑、审批、归档、权限变更和问答调用等关键操作。
- 敏感信息检测：识别身份证号、手机号、财务数据、密钥、内部机密词等敏感内容，并在发布前提示或拦截。

#### 功能优先级与验收口径

| 功能模块 | 优先级 | 规划版本 | 验收标准 |
|----------|--------|----------|----------|
| 用户注册登录 | P0 | V1 | 用户可注册、登录、退出；未登录访问受保护接口返回 401；登录态可恢复 |
| 企业和团队空间 | P0 | V1 | 系统内存在企业、部门/团队数据；普通用户只能访问自己有权限的空间内容 |
| 文档上传 | P0 | V1 | 用户可上传 Markdown/文本文件，系统可解析为可编辑正文并生成文档 |
| 文档在线管理 | P0 | V1 | 支持新建、编辑、保存、归档、恢复、评论、版本记录与状态流转 |
| 权限控制 | P0 | V1 | 非管理员不能写入其他部门文档；私有部门文档不会被跨部门成员看到 |
| 全文搜索 | P0 | V1 | 可按标题、标签、正文检索；搜索结果遵守当前用户权限范围 |
| RAG 问答 | P1 | V2 | 用户可自然语言提问，系统基于召回文档生成回答并流式输出 |
| 对话历史 | P1 | V2 | 同一会话内可保留上下文、问题、回答、引用和执行轨迹 |
| 引用来源 | P1 | V2 | 每条回答至少返回来源文档、版本或片段引用；无来源时明确提示未找到依据 |
| 管理员后台 | P1 | V1/V2 | 管理员可查看用户、部门、文档、审批、指标，后续可管理权限和配置 |
| Docker 部署 | P1 | V1/V4 | 开发期可用 Docker Compose 启动依赖；企业化阶段可完整容器化部署 |
| 自动总结文档 | P2 | V2 | 文档上传或保存后可生成摘要、关键词和推荐标签 |
| 文档版本比较 | P2 | V2 | 可查看两个版本的内容差异和变更摘要 |
| 会议纪要提取 | P2 | V3 | 可从会议文本中提取议题、结论、待办和责任人 |
| 多 Agent 审核流程 | P2 | V3 | 审核 Agent 可辅助检查格式、重复内容、敏感信息与合规风险 |
| 敏感信息检测 | P2 | V3 | 发布前能识别常见敏感字段并提示、拦截或要求人工确认 |
| 知识库更新提醒 | P3 | V3/V4 | 订阅者可收到文档新增、更新、审批通过或关键知识变化提醒 |
| 操作日志 | P3 | V3/V4 | 可记录并查询登录、上传、编辑、审批、权限变更和问答调用等行为 |

#### 非功能需求

- 安全性：认证、鉴权、租户隔离、部门隔离和文档级权限必须在 API 与检索层同时生效。
- 可追溯性：文档版本、审批记录、评论、问答引用和操作日志需要形成完整链路。
- 可扩展性：RAG、Agent、MCP、A2A、Skills 等能力应以模块边界预留，避免 V1 阶段过度耦合。
- 可部署性：本地开发优先保证一键启动，企业化阶段补齐容器化、配置隔离、迁移和监控。
- 可演示性：每个版本都应有明确的端到端主链路，避免只完成底层模块而无法展示业务价值。

### 1.4 项目价值总结

| 价值点 | 量化效果（以200人团队估算） |
|--------|---------------------------|
| 减少员工查找文档时间 | 每人每周节约约1.5小时 |
| 降低知识流失风险 | 关键文档覆盖率从~30%提升至~80% |
| 提高问答效率 | 常见问题无需咨询同事，Agent 直接回答 |
| 规范文档管理流程 | 审批发布机制确保文档质量 |
| 数据安全隔离 | 部门间数据零泄露风险 |

### 1.5 面试定位

一套以 Agent 驱动为核心的企业级知识底座，涵盖全栈工程能力与 AI 系统设计能力。面试时可直接表述为：**"我设计和实现了一套服务200人团队规模的企业内部知识管理与智能问答平台，涵盖文档管理、权限隔离、多Agent协作检索与问答、全链路可观测与成本控制。"**


---

## 二、技术栈全景

```
┌─────────────────────────────────────────────────────────────┐
│                      前端层 (React)                          │
│  企业级控制台  文档编辑器  审批中心  看板  知识问答浮层      │
└───────────────────────────┬─────────────────────────────────┘
                            │ SSE / REST API
┌───────────────────────────▼─────────────────────────────────┐
│                      网关层                                  │
│                 认证鉴权 / 路由分发 / 限流                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                   Agent 编排层 (LangGraph)                    │
│                                                              │
│   ┌────────────┐  ┌────────────┐  ┌────────────┐           │
│   │ 路由Agent  │  │ 编排Agent  │  │ 监督Agent  │           │
│   └─────┬──────┘  └─────┬──────┘  └─────┬──────┘           │
│         │               │               │                   │
│   ┌─────▼───────────────▼───────────────▼──────┐           │
│   │            Worker Agent 池                  │           │
│   │  检索Agent  问答Agent  审核Agent  接入Agent │           │
│   └─────┬───────────────┬───────────────┬──────┘           │
│         │               │               │                   │
│   ┌─────▼───────┐ ┌─────▼───────┐ ┌─────▼───────┐         │
│   │  Tools层     │ │  Skills层    │ │ Memory层    │         │
│   └─────────────┘ └─────────────┘ └─────────────┘           │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                      RAG 引擎层                              │
│  文档切割  向量库  多路召回  Query改写  重排序  幻觉规避     │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                       数据层                                 │
│   PostgreSQL  Redis  ChromaDB/FAISS  对象存储 (S3/MinIO)    │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、Agent 编排层 — LangGraph 多Agent协作

### 3.1 设计范式

采用 **Orchestrator-Worker** 为主、**Pipeline** 为辅的混合范式：

- **路由Agent**（Orchestrator）：接收用户输入，判断意图类型，分发到对应Worker或组合多个Worker
- **编排Agent**（Plan-then-Execute）：对复杂任务拆分为子任务，生成执行图
- **监督Agent**（Supervisor）：监控各Worker执行状态，处理异常与超时重试
- **Worker Agent 池**：

| Agent | 职责 |
|-------|------|
| 检索Agent | 根据问题调用多路召回管道，返回相关文档片段 |
| 问答Agent | 基于召回内容组织答案，附带引用，执行幻觉校验 |
| 审核Agent | 检查文档内容合规性、格式规范、敏感信息 |
| 接入Agent | 解析上传文档，自动提取摘要、关键词、生成标签 |
| 分析Agent | 对多文档进行交叉对比分析，生成总结报告 |

### 3.2 任务拆分机制

```
用户提问："对比公司Q1和Q2的招聘数据变化趋势"

路由Agent 识别意图 → 需要多文档对比分析
    ↓
编排Agent 计划生成：
    Step1: 检索Agent → 查找Q1招聘报告 + Q2招聘报告
    Step2: 检索Agent → 查找历史对比模板
    Step3: 分析Agent → 交叉对比两份报告，提取关键指标
    Step4: 问答Agent → 组织成对比回答，附数据来源
    ↓
监督Agent 检查各步骤执行状态，超时/异常时重试
    ↓
汇总输出
```

### 3.3 记忆机制

分层记忆架构：

| 记忆层级 | 存储形式 | 生命周期 | 用途 |
|----------|----------|----------|------|
| 短期记忆 | 对话上下文窗口（Token窗口） | 单次会话 | 保持对话连贯性 |
| 工作记忆 | Redis（结构化缓存） | 单次会话 | Agent执行过程中的中间状态 |
| 长期记忆 | 向量数据库 + PostgreSQL | 持久化 | 用户偏好、历史查询模式、知识积累 |
| 会话记忆 | PostgreSQL（会话表） | 按策略清理 | 支持"上次问到这里"的后续对话 |

实现方式：
- LangGraph 的 `MemorySaver` 管理短期与工作记忆
- 长期记忆通过 Agent Tool 访问向量库写入/读取
- 会话记忆通过 REST API 持久化到 PostgreSQL

### 3.4 Multi-Agent 协作协议

```
[用户] → [路由Agent] → LLM Call (Function Calling)
    │
    ├──→ 普通检索 → 检索Agent (单Worker)
    │
    ├──→ 综合分析 → 编排Agent 拆分 → 并行分发多个Worker
    │       ↓
    │   A2A Protocol → [检索Agent] ↔ [分析Agent] ↔ [问答Agent]
    │       ↓
    │   汇总层 (Aggregator) → 整合结果
    │
    └──→ 需要人工介入 → 监督Agent 挂起任务，发送通知
```

- **Function Calling**：Agent 通过 LLM 的 tool call 能力选择工具
- **Tools**：每个 Worker 暴露一组工具接口（搜索、读取、写入、审核）
- **MCP (Model Context Protocol)**：标准化 Agent 与外部工具的接口描述规范
- **A2A (Agent-to-Agent)**：Agent 间通信协议，支持同步请求/异步事件推送
- **Skills**：可复用的技能模块，如"文档摘要技能"、"对比分析技能"，可挂载到任意 Agent
- **SSE传输协议**：前端与后端之间流式传输 Agent 执行过程与结果

---

## 四、RAG 引擎层

### 4.1 文档切割策略

多种切割策略组合使用，根据文档类型自动选择：

| 策略 | 适用场景 | 实现 |
|------|----------|------|
| 语义切分 | 长文本、报告、文章 | 基于句子嵌入相似度切分，保持段落语义完整 |
| 递归字符切分 | Markdown、代码文档 | 按标题层级（# → ## → ###）递归分割 |
| 固定大小切分 | PDF、纯文本 | 固定 token 数（512/1024），带重叠窗口（overlap=128） |
| 结构化解析 | Word/PDF 表格 | 提取表格结构为结构化数据块，保留行列关系 |

切割后每个 Chunk 携带元数据：文档ID、标题路径、段落层级、切割策略、原始顺序位置。

### 4.2 向量数据库选型与设计

**选型：ChromaDB（开发期）/ FAISS + PostgreSQL pgvector（生产期）**

- 索引类型：IVF_FLAT（开发期）→ HNSW（生产期，支持百万级）
- 向量维度：根据 Embedding 模型决定（如 text-embedding-3-small: 1536d）
- 存储结构：

```json
{
  "id": "chunk_uuid",
  "vector": [0.123, -0.456, ...],
  "metadata": {
    "doc_id": "doc_uuid",
    "title": "Q2招聘报告",
    "section": "3.2 技术岗位分析",
    "chunk_strategy": "recursive",
    "chunk_index": 5,
    "timestamp": "2026-07-01"
  },
  "text": "原始文本内容..."
}
```

### 4.3 检索优化技术栈

```
用户查询
    │
Query 预处理：
    ├── Query 改写：LLM 对模糊/简化的问题进行扩写、纠错、分解
    │               例："Q2情况" → "XX公司2026年第二季度招聘数据"
    │
    ├── Query 扩展：同义词/相关概念扩展，增加召回覆盖面
    │
    └── Query 分解：复杂问题拆为多个子查询
    │
    ▼
多路召回（并行执行）：
    ├── 语义召回：向量相似度检索（Top-K）
    ├── 关键词召回：BM25 全文检索
    ├── 元数据过滤：按时间/分类/作者精确筛选
    └── 图召回：基于文档引用关系扩散（文档A提到了文档B）
    │
    ▼
融合重排序：
    └── RRF (Reciprocal Rank Fusion) 合并多路结果
        → Cohere/Cross-Encoder 重排序（精排 Top-N）
    │
    ▼
后处理：
    ├── 引用溯源：每个返回内容携带来源文档ID + 原文位置
    ├── 置信度打分：语义相似度 + 重排序分数 加权
    └── 上下文窗口裁剪：按 Token 上限裁剪至 LLM 可接受长度
```

### 4.4 Query 改写详解

| 改写类型 | 触发条件 | 示例 |
|----------|----------|------|
| 补全 | 输入过短（<3词） | "权限" → "系统用户文档管理权限如何配置" |
| 纠错 | 检测到拼写/语法问题 | "怎摸设置审批留成" → "怎么设置审批流程" |
| 分解 | 含并列结构 | "Q1和Q2的招聘数据" → "Q1招聘数据" + "Q2招聘数据" |
| 反查 | 含代词/指代 | "它的审核流程" → 检索上文"它"指代的文档 |

### 4.5 幻觉规避体系

多层防御策略：

```
第一层：检索质量
    → 保证多路召回命中的文档片段与问题高度相关
    → 相关性阈值过滤（低于阈值不返回）

第二层：引用强制
    → Agent 回答时必须附上引用来源（文档ID + 原文片段）
    → 无引用不输出

第三层：事实校验
    → 将回答拆分为"事实声明"列表
    → 每个声明回检索库验证是否可找到支撑原文
    → 未验证声明在回答中标注"待核实"

第四层：置信度分级
    → 高置信度：有原文直接支撑 → 直接展示
    → 中置信度：有间接支撑 → 展示但标注"基于相关文档推断"
    → 低置信度：无支撑 → 返回"未找到相关信息，建议..."
```

---

## 五、内部通讯协议

### 5.1 Function Calling（Agent → LLM Tool Call）

LLM 通过 Function Calling 声明要调用的工具，Agent 执行后返回结果：

```json
{
  "tools": [
    {
      "name": "search_documents",
      "description": "搜索知识库文档",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {"type": "string"},
          "top_k": {"type": "integer"},
          "filters": {"type": "object"}
        }
      }
    }
  ]
}
```

### 5.2 MCP (Model Context Protocol)

标准化 Agent 与外部工具的接口契约，每个工具注册为 MCP Resource：

```
MCP Resource 注册示例：
  Resource: knowledge_db.search
  Schema: { query: str, top_k: int, filters: dict } → { results: list }
  Auth: header x-api-key
  RateLimit: 60 req/min
```

### 5.3 A2A (Agent-to-Agent)

Agent 间通信协议，支持两种模式：

| 模式 | 适用场景 | 协议 |
|------|----------|------|
| 同步请求 | 需要立即返回结果 | HTTP/gRPC 请求-响应 |
| 异步事件 | 长任务/多Agent协作 | Redis Pub/Sub + SSE 推送 |

**通信数据结构**：
```json
{
  "from": "router_agent",
  "to": "retrieval_agent",
  "task_id": "uuid",
  "type": "query",
  "payload": {
    "query": "对比Q1和Q2招聘数据",
    "context": {"session_id": "xxx"},
    "timeout": 30
  },
  "protocol": "a2a/v1"
}
```

### 5.4 SSE 传输协议

用于前端实时展示 Agent 执行过程：

```
事件流示例：
  event: agent_start
  data: {"agent": "router", "task_id": "uuid"}

  event: agent_thinking
  data: {"agent": "router", "thought": "需要检索两份文档..."}

  event: tool_call
  data: {"agent": "retrieval", "tool": "search_documents", "args": {...}}

  event: agent_result
  data: {"agent": "retrieval", "result": {...}}

  event: agent_complete
  data: {"agent": "router", "final": "回答内容..."}
```

前端通过 `EventSource` 或 `fetch + ReadableStream` 消费。

---

## 六、Skills 模块化设计

Skills 是可复用的能力单元，每个 Agent 可按需挂载：

| Skill | 功能 | 挂载对象 |
|-------|------|----------|
| `skill_document_summarize` | 文档自动摘要 | 接入Agent、问答Agent |
| `skill_compare_analysis` | 多文档对比分析 | 分析Agent |
| `skill_code_review` | 代码片段审查（扩展） | 审核Agent |
| `skill_forward_reference` | 引用关系探测（文档A→文档B） | 检索Agent |
| `skill_sensitive_detect` | 敏感信息检测 | 审核Agent |
| `skill_template_match` | 格式模板匹配 | 审核Agent |

---

## 七、版本规划

### V1 — 基石（4周）
目标：跑通企业知识库最小可用闭环，即用户登录后可以完成文档上传/编辑、权限隔离、审批发布、全文搜索和基础管理。

交付范围：
- 用户注册登录：JWT 鉴权、登录态恢复、未登录拦截。
- 企业和团队空间：企业租户、部门/团队、用户角色、部门默认权限。
- 文档上传与在线管理：Markdown/文本上传解析、新建、编辑、保存、归档、恢复、评论和版本记录。
- 文档发布流程：编辑→提交审核→通过/驳回，保留审批记录和驳回原因。
- 权限控制：租户隔离、部门隔离、公开可见、作者/管理员编辑。
- 全文搜索：标题、标签、正文检索，搜索结果遵守权限范围。
- 管理员后台基础：用户、部门、文档、审批和数据指标的基础查看。
- 部署基础：FastAPI + React + PostgreSQL/SQLite 开发模式，Docker Compose 启动 PostgreSQL、Redis、ChromaDB、MinIO 等依赖。

验收标准：
- 普通成员不能访问或编辑无权限文档，管理员可以处理审批。
- 一篇文档可以从创建、保存、提交审批、审批通过到搜索查看完整流转。
- 归档文档默认不进入搜索和审批列表，恢复后回到草稿状态。
- 前端包含控制台、文档库、编辑页、审批中心、知识问答入口、数据看板、管理后台入口。

### V2 — 智能（+3周）
目标：让知识库可以被自然语言提问，并保证回答可追溯、过程可观察。

交付范围：
- LangGraph 路由Agent、检索Agent、问答Agent 初步可用。
- RAG 问答：文档切割、向量化、语义召回、关键词召回、元数据过滤和 RRF 融合。
- Query 改写与重排序：支持短问题补全、错别字纠正、复杂问题拆解。
- 引用来源：回答携带来源文档、版本、片段和置信度。
- 对话历史：同一会话内保留问题、回答、引用和基础 Trace。
- SSE 流式输出：前端逐步展示回答和 Agent 执行过程。
- 自动总结文档：上传或保存后生成摘要、关键词和推荐标签。
- 文档版本比较：支持版本差异查看和变更摘要展示。

验收标准：
- 用户可以在"知识问答"浮层中提问，并收到带引用来源的流式回答。
- 检索结果不会越权返回其他部门私有文档。
- 至少支持一篇文档的自动摘要和两个版本的差异对比。

### V3 — 完善（+3周）
目标：形成可对外演示的多 Agent 协作版本，覆盖审核、分析、纪要和通知等高价值场景。

交付范围：
- 编排Agent（Plan-then-Execute）支持复杂任务拆分和多 Worker 协作。
- A2A Agent 间通信：支持同步请求和异步事件。
- MCP 标准化工具接口：封装搜索、读取、写入、审核等工具能力。
- Skills 模块挂载：摘要、对比分析、模板匹配、敏感信息检测、引用关系探测。
- 多 Agent 审核流程：审核 Agent 辅助检查格式、重复内容、敏感信息和合规风险。
- 会议纪要提取：从会议文本中提取议题、结论、待办、责任人和截止时间。
- 知识库更新提醒：文档新增、更新、审批通过或关键内容变化时通知相关团队。
- 操作日志：记录登录、上传、编辑、审批、归档、权限变更和问答调用。
- 数据看板增强：展示文档增长、阅读活跃、问答命中率、审核风险和高频主题。

验收标准：
- 审批中心可以看到 Agent 辅助审核结果，并保留人工最终决策。
- 会议纪要提取可以生成结构化待办。
- 管理员可以查询关键操作日志和知识库更新记录。

### V4 — 企业化（+2周，可选）
目标：补齐生产部署、稳定性、安全和运维能力，达到企业内部试运行标准。

交付范围：
- 监督Agent：异常重试、超时处理、任务降级和失败告警。
- 长期记忆机制：Redis + PostgreSQL + 向量库持久化，支持会话回溯和用户偏好。
- 完整 Docker 部署：前端、后端、数据库、缓存、向量库、对象存储统一编排。
- 权限粒度细化：文档级 ACL、空间管理员、只读/可写/可审批等角色策略。
- 数据迁移与备份：Alembic 迁移、数据库备份、对象存储备份和恢复策略。
- 测试覆盖：单元测试、集成测试、权限测试、检索测试和端到端主链路测试。
- API 文档与运维文档：自动生成 OpenAPI 文档，并补充部署、配置、排障说明。

验收标准：
- 新环境可以通过文档完成部署，并通过健康检查。
- 权限、审批、搜索、问答、日志和通知主链路均有测试覆盖。
- 出现 Agent 或工具异常时，系统能记录 Trace 并给出可恢复的用户提示。

---

## 八、前端页面需求（去AI味，企业级风）

### 整体设计原则
- 主色调：深蓝 (#1a365d) / 灰蓝 (#2d5a8e) + 白色背景
- 左导航栏收窄（图标+文字标签），不遮挡内容区
- 大面积留白，信息密度适中
- 所有AI能力以"工具"形式存在，不突出"AI"身份

### 页面清单

#### 1. 控制台 / 首页
- 顶部：全局搜索框（居左）+ 用户头像/设置（居右）
- 左侧：导航菜单（文档库、发布审批、知识问答、数据看板、管理后台）
- 内容区：最近浏览文档、待审批文档、热门文档推荐
- 页面右下角：一个不起眼的"问知识库"入口图标（对话气泡样式，不加"Ai"字样）

#### 2. 文档库列表页
- 面包屑导航（文档库 > 部门/分类）
- 列表/网格切换展示
- 行数据：文档标题 + 作者 + 更新时间 + 版本号 + 状态 + 部门/空间
- 筛选栏：分类标签 + 状态 + 时间范围 + 已归档
- 操作：打开、归档、恢复、按权限隐藏不可操作按钮

#### 3. 文档编辑/详情页
- 左侧：文档内容（编辑/预览切换）
- 右上角：保存/提交审批/归档/恢复/上传解析/预览切换
- 右侧：文档状态、审批记录、协作评论、版本记录
- 归档态：禁止编辑、提交审批和新增评论，只允许恢复或查看历史信息
- 详情模式：底部或侧栏展示审批记录、评论、版本和引用关系

#### 4. 审批中心
- 待审批列表：文档标题 + 提交人 + 提交时间 + 版本变动摘要
- 操作：通过/驳回，驳回需填原因
- 已审批列表带最终结果与审批人
- 进阶：展示审核 Agent 的格式检查、敏感信息检测和风险提示

#### 5. 知识问答浮层（非AI感入口）
- 触发方式：点击"问知识库"图标，右侧滑入浮层面板
- 输入框在面板底部，历史对话在上方可滚动
- 每条回答末尾带引用小标记，点击展开对应原文
- 面板标题显示"知识问答"，不写"AI助手"
- SSE 流式输出逐字展示 Agent 思考与回答过程
- 关闭面板不保留悬浮气泡
- 支持对话历史、引用来源、执行 Trace 展开和无依据回答提示

#### 6. 数据看板
- 卡片布局：文档总数、本周新增、活跃用户、Top文档
- 简单趋势图（周维度文档新增量/阅读量）

#### 7. 管理员后台
- 用户管理：查看用户、角色、所属企业与部门/团队空间
- 空间管理：维护企业、部门、团队和空间权限
- 文档管理：按状态、部门、作者、标签查看文档，支持管理员级归档和恢复
- 审批管理：查看待审批、已审批、驳回原因和审核风险
- 系统配置：维护部署配置、检索参数、通知策略和安全策略
- 审计日志：查询登录、上传、编辑、审批、归档、权限变更和问答调用记录

---

## 九、项目文件结构

```
knowledge-platform/
├── frontend/                  # React + TypeScript
│   ├── src/
│   │   ├── pages/            # 页面组件
│   │   ├── components/       # 通用组件
│   │   ├── hooks/            # 自定义hooks
│   │   ├── services/         # API 调用 + SSE
│   │   └── store/            # 状态管理
│   └── package.json
├── backend/                   # FastAPI + LangGraph
│   ├── api/                  # REST 路由
│   ├── agents/               # LangGraph Agent 定义
│   │   ├── router_agent.py
│   │   ├── retrieval_agent.py
│   │   ├── qa_agent.py
│   │   ├── review_agent.py
│   │   └── orchestrator.py
│   ├── rag/                  # RAG 引擎
│   │   ├── chunker/          # 文档切割
│   │   ├── embedding/
│   │   ├── retrieval/        # 多路召回
│   │   ├── reranker/         # 重排序
│   │   └── hallucination/    # 幻觉规避
│   ├── memory/               # 记忆层
│   ├── skills/               # Skills 模块
│   ├── mcp/                  # MCP 协议实现
│   ├── a2a/                  # A2A Agent间通信
│   └── requirements.txt
├── db/                       # 数据库迁移
└── docker-compose.yml
```

---

## 十、优化与补充设计

### 10.1 Token 成本控制与预算管理

多Agent系统在生产环境中面临的核心问题是LLM调用成本不可控。加入以下机制：

**预算管理器 (Budget Manager)**

```
Agent 收到任务
    ↓
预算管理器 预估 Token 消耗（基于任务类型 + 历史均值）
    ├── 低于预算阈值 → 正常执行
    ├── 超预算但可降级 → 切换到轻量模型（如 gpt-4o-mini）
    └── 严重超预算 → 拒绝执行，返回"该查询超出预算，请联系管理员"
    ↓
执行过程中实时监控 Token 消耗
    ↓
执行完毕 → 记录实际用量 → 更新预算池余额
```

**实现细节：**
- 预算粒度：全局预算 / 部门预算 / 用户日预算 三级
- Token 预估公式：`预估消耗 = 输入Token(查询长度 × 召回文档数) + 输出Token(预期回答长度)`
- 降级策略表：

| 原模型 | 降级模型 | 适用场景 | 节省比例 |
|--------|----------|----------|----------|
| gpt-4o | gpt-4o-mini | 普通检索问答 | ~60% |
| gpt-4o | claude-3-haiku | 文档摘要 | ~55% |
| gpt-4o-mini | 本地模型 | 审核/分类 | ~90% |

- 预算告警：预算余额低于20%时通知管理员

**面试亮点：** 体现对生产环境中AI系统成本问题的工程化思考。

---

### 10.2 可观测性 — Agent Trace 全链路追踪

多Agent系统中，一条用户请求可能经过3~5个Agent、调用7~10个工具、往返多次LLM。没有trace就无法排查问题。

**Trace 数据结构：**

```json
{
  "trace_id": "request_uuid",
  "session_id": "session_uuid",
  "user_id": "user_uuid",
  "start_time": "2026-07-27T20:00:00Z",
  "end_time": "2026-07-27T20:00:05Z",
  "duration_ms": 5120,
  "status": "success",
  "spans": [
    {
      "span_id": "span_001",
      "parent_span_id": null,
      "agent": "router_agent",
      "operation": "intent_classification",
      "start_time": "...",
      "duration_ms": 320,
      "input": {"query": "Q1招聘情况"},
      "output": {"intent": "retrieval", "confidence": 0.92}
    },
    {
      "span_id": "span_002",
      "parent_span_id": "span_001",
      "agent": "retrieval_agent",
      "operation": "multi_recall",
      "start_time": "...",
      "duration_ms": 1500,
      "details": {
        "vector_recall_count": 10,
        "bm25_recall_count": 8,
        "rerank_top": 5,
        "avg_similarity": 0.85
      }
    }
  ]
}
```

**前端展示：**
- 知识问答浮层中可展开"查看执行详情"
- 以时间线的形式展示每个Agent的执行步骤与耗时
- 失败步骤标红，hover显示错误信息

**面试亮点：** 展现对分布式系统可观测性的工程素养。

---

### 10.3 多租户数据隔离

企业场景下，不同部门的数据必须严格隔离。这是企业级最基本要求。

**隔离级别设计：**

| 级别 | 实现方式 | 适用场景 |
|------|----------|----------|
| 数据库级隔离 | 每个租户独立 database | 高安全要求（如金融/医疗） |
| Schema级隔离 | 同一 database，不同 schema | 中型企业 |
| 行级隔离 | 所有数据同表，通过 tenant_id 行级过滤 | 中小团队 |

本项目采用 **行级隔离 + 文档级ACL**：

**行级隔离：**
- 每张业务表保留 `tenant_id` 和 `department_id` 字段
- 数据库层通过 Row-Level Security (RLS) 自动过滤
- API 层中间件注入当前用户所属租户/部门

**文档级ACL：**
```json
{
  "doc_id": "doc_uuid",
  "owner": "user_uuid",
  "permissions": {
    "read": ["user_uuid_1", "dept_uuid_tech"],
    "write": ["user_uuid_1", "user_uuid_2"],
    "admin": ["user_uuid_1"]
  }
}
```

**检索Agent的隔离约束：**
- 检索Agent 发起搜索时，自动携带当前用户 `tenant_id` + `department_id`
- 召回管道在最顶层应用过滤条件：`WHERE tenant_id = ? AND (department_id = ? OR doc_visibility = 'public')`
- 确保一个部门的成员绝对看不到另一个部门的私有文档

**面试亮点：** 展示对企业级SaaS架构中多租户问题的理解。

---

### 10.4 技术决策记录 (tech_decisions.md)

面试官看的不是代码行数，而是你在技术选型上做过哪些权衡。建议在项目根目录维护 `tech_decisions.md`，记录每个重要决策的背景、选型方案、取舍理由。

**模板示例：**

```markdown
# 技术决策记录

## ADR-001: 为什么选择 ChromaDB 而非 Milvus

- **背景**：需要为文档库构建向量检索能力
- **选项**：ChromaDB / FAISS / Milvus / Pinecone
- **选择**：开发期用 ChromaDB，生产期切换 FAISS + pgvector
- **理由**：
  - ChromaDB 零配置、轻量，适合原型快速迭代
  - FAISS 索引性能更好，支持 HNSW 算法
  - pgvector 与 PostgreSQL 同一数据源，减少运维复杂度
  - Milvus 太重，对于团队规模项目过度设计
  - Pinecone 依赖第三方服务，不适合内部部署
- **取舍**：开发效率优先，性能优化延后到 V2

## ADR-002: RRF vs Learning to Rank 重排序选型

（同理记录）
```

**建议记录的重点决策：**
1. 向量库选型（ChromaDB vs FAISS vs Milvus）
2. 重排序算法选型（RRF vs Cohere vs Cross-Encoder）
3. 文档切割策略选型（语义切分 vs 递归切分）
4. 多Agent编排范式选型（Orchestrator-Worker vs Pipeline vs Chat）
5. 前端状态管理选型（Redux vs Zustand vs Context）
6. SSE vs WebSocket 传输协议选型
7. 记忆机制持久化方案选型（Redis vs PostgreSQL vs 向量库）

**面试亮点：** 这是面试中最直接的加分项——"为什么做这个选择"比"做了什么"更能体现技术深度。
